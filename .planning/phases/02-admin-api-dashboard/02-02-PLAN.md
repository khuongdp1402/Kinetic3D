---
wave: 2
depends_on: ["02-01"]
files_modified:
  - "src/Kinetic3D.WebUI/package.json"
  - "src/Kinetic3D.WebUI/app/layout.tsx"
  - "src/Kinetic3D.WebUI/app/(admin)/layout.tsx"
  - "src/Kinetic3D.WebUI/app/(admin)/admin/products/page.tsx"
  - "src/Kinetic3D.WebUI/app/(admin)/admin/products/new/page.tsx"
  - "src/Kinetic3D.WebUI/components/admin/Sidebar.tsx"
  - "src/Kinetic3D.WebUI/components/admin/VariantMatrix.tsx"
  - "src/Kinetic3D.WebUI/components/admin/ImageUpload.tsx"
autonomous: true
---

# Plan 02-02: Next.js Admin Dashboard and Variant Matrix

## Goal
Set up the Next.js frontend application structure and implement the Admin dashboard for managing products, variants, and flash sales, utilizing the APIs created in Plan 02-01.

## Tasks

<task>
<description>Initialize Next.js application</description>
<read_first>
- "Kinetic3D.sln"
</read_first>
<action>
Run:
`npx -y create-next-app@latest src/Kinetic3D.WebUI --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --use-npm`
Wait for installation.
</action>
<acceptance_criteria>
- `src/Kinetic3D.WebUI/package.json` exists.
- `src/Kinetic3D.WebUI/app/layout.tsx` exists.
</acceptance_criteria>
</task>

<task>
<description>Create Admin Route Group and Sidebar</description>
<read_first>
- "src/Kinetic3D.WebUI/app/layout.tsx"
</read_first>
<action>
Create `src/Kinetic3D.WebUI/app/(admin)/layout.tsx`.
Create `src/Kinetic3D.WebUI/components/admin/Sidebar.tsx`.
The sidebar should have links to Dashboard, Products, Categories, and Flash Sales.
Render the `<Sidebar />` inside `(admin)/layout.tsx`.
</action>
<acceptance_criteria>
- `src/Kinetic3D.WebUI/app/(admin)/layout.tsx` contains `<Sidebar />`.
</acceptance_criteria>
</task>

<task>
<description>Implement Product Creation Form and MinIO Upload</description>
<read_first>
- "src/Kinetic3D.WebUI/app/(admin)/admin/products/new/page.tsx"
</read_first>
<action>
Create `src/Kinetic3D.WebUI/app/(admin)/admin/products/new/page.tsx`.
Create `src/Kinetic3D.WebUI/components/admin/ImageUpload.tsx`.
The ImageUpload component should make a `POST` request to `http://localhost:5000/api/uploads/presigned-url` to get the URL, then use `fetch` with `PUT` to upload the selected file directly to MinIO.
</action>
<acceptance_criteria>
- `ImageUpload.tsx` contains `fetch` calling `presigned-url` and a `PUT` request.
</acceptance_criteria>
</task>

<task>
<description>Implement Variant Matrix Component</description>
<read_first>
- "src/Kinetic3D.WebUI/components/admin/VariantMatrix.tsx"
</read_first>
<action>
Create `src/Kinetic3D.WebUI/components/admin/VariantMatrix.tsx`.
Implement an editable grid (table). Rows correspond to variants, and columns for `SKU`, `Price`, and `Stock`. Provide an "Add Variant" button that allows adding new rows with dynamic attributes stored in a JSON format.
</action>
<acceptance_criteria>
- `VariantMatrix.tsx` contains a `<table>` with editable inputs for SKU, Price, and Stock.
</acceptance_criteria>
</task>

<task>
<description>Implement Flash Sale Management</description>
<read_first>
- "src/Kinetic3D.WebUI/app/(admin)/admin/flash-sales/page.tsx"
</read_first>
<action>
Create `src/Kinetic3D.WebUI/app/(admin)/admin/flash-sales/page.tsx`.
Implement a form to set Flash Sale Start Time, End Time, and select products from the database.
</action>
<acceptance_criteria>
- `flash-sales/page.tsx` exists and contains date/time inputs.
</acceptance_criteria>
</task>

## Verification
- Run `npm run dev` in `src/Kinetic3D.WebUI`.
- Access `http://localhost:3000/admin/products/new` and ensure the form and Variant Matrix render properly.

## Requirements Covered
- ADMIN-01
- ADMIN-02
- ADMIN-03
- ADMIN-04
